# OS artifacts
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini

# Editor directories
.vscode/
.idea/
*.sublime-workspace

# Local dev server caches
.cache/
node_modules/

# Build output (if you ever add a build step)
dist/
build/

# Uploaded test images (keep assets/backgrounds/ tracked but ignore local test images)
assets/backgrounds/*.jpg
assets/backgrounds/*.jpeg
assets/backgrounds/*.png
assets/backgrounds/*.webp
assets/backgrounds/*.gif
# But keep the .gitkeep placeholder
!assets/backgrounds/.gitkeep
